---
const { url = '#', style = ''  } = Astro.props
const webpUrl = url.replace(/\.(png|jpg)$/i, '.webp');
---

<video class={style}
controls preload autoplay muted playsinline loop  >
  <source src={ url } type="video/mp4">
    Your browser does not support the video tag.
</video>


